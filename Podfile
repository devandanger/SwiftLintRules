# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftLintRules' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftLint'
  pod 'RxSwift', '4.0'
  pod 'RxCocoa', '4.0'

  # Pods for SwiftLintRules

  target 'SwiftLintRulesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftLintRulesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
